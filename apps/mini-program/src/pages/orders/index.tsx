import { View, Text } from "@tarojs/components";

export default function OrdersPage() {
  return <View className="page"><View className="card"><Text>订单 / 全部 / 待处理 / 已支付 / 已预约 / 已完成</Text></View></View>;
}
